[project]
name = "semantic-poc"
version = "0.1.0"
description = "DDD FastAPI service"
requires-python = ">=3.11"
dependencies = [
  "fastapi[all]>=0.116.1",
  "pydantic>=2.3.0",
  "python-dotenv",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
